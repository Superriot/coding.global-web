1. Variable die überschrieben werden kann erstellen mit user array |
2. kucken ob die user.json datei da ist.
2.1 wen die datei da ist werte aus der datei in user array speichern. 
3. wen in der variable keine user vorhanden sind 
3.1 datei erstellen mit dem aktuellen user als array
3.2 res.statuts return user 
4. wen die variable da ist mit user dan mit find über den array gehen ob die email über ein stimmt die gesendet wurde die im array ist
5. wen der user da ist 
5.1 return res.status() user already exist 
5.2 wen der user nicht existiert wird der user in der json datei erstellt + die user die schon drin waren 

